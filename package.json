{
  "name": "sector-tasks-obsidian",
  "version": "1.2.0",
  "description": "Task board over a single Tasks-plugin-format Markdown file, grouped by configurable sector tags.",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "node --test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.28.1",
    "obsidian": "^1.7.2",
    "typescript": "^5.7.2"
  }
}
